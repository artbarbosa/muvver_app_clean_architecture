import 'package:flutter/material.dart';

class MessageErrorPage extends StatelessWidget {
  const MessageErrorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Error ao adicionar'),
      ),
    );
  }
}
